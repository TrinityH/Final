#pragma once

// Prototype functions
void greetUser();

std::string getUserName();

void strangerName(std::string name);

void goToCoord(int x, int y);

void color(int color);

void showLoadingScreen();

void credits();

// Prototype classes
class thePills
{
 // Prototype string objects and give them public access
public:
	std::string bluePill;
public:
	std::string redPill;
};

class cars
{
// Prototype string objects and give them public access
public:
	std::string mustang;
public:
	std::string corvette;
};

